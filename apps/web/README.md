# Web App Workspace

Static browser-first data interaction workspace for Cloudflare Pages.

## Included in This Stage

- Dataset inventory panel (domain, kind, freshness, row count)
- Global filters (entity, plan period, metric family, confidence, source document)
- Record drilldowns with provenance/evidence references
- Saved views (localStorage)
- Filtered export to JSON or CSV

## Local Preview

Open `apps/web/index.html` in a browser or run a static file server.

## Runtime Config

- Default config: `apps/web/config/default.json`
- Deploy-time config: `apps/web/config/runtime.json` (generated by workflow)
- URL overrides for validation: `?apiBaseUrl=...&artifactBaseUrl=...&environment=...`
  (enabled only for local contexts or when `enableQueryOverrides=true` in config)

## Smoke Test

```bash
python scripts/web/smoke_test.py --base-dir apps/web
```
